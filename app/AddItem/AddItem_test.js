'use strict';

describe('myApp.AddItem module', function() {

  beforeEach(module('myApp.AddItem'));

  describe('AddItem controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var AddItemCtrl = $controller('AddItemCtrl');
      expect(AddItemCtrl).toBeDefined();
    }));

  });
});